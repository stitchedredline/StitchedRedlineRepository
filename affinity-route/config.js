/* Paste your Google Apps Script Web App URL between the quotes, commit, done.
   Leave it empty and the driver app still works fully offline — you just tap
   doors yourself instead of getting QR pings. */
window.AFFINITY_API = "";
window.AFFINITY_PROPERTY = "affinity1";
window.AFFINITY_PROPERTY_NAME = "Affinity Waste — Charlotte";
